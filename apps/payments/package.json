{
  "name": "@bet-platform/payments",
  "version": "0.2.0",
  "private": true,
  "description": "Integração Stone/Pagar.me com split de pagamento (modelo Hotmart) — vendorizado de BelaBet/vc para uso pelo apps/api.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node --env-file=../../.env dist/server.js",
    "dev": "tsc --watch & node --env-file=../../.env --watch dist/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0",
    "@types/express": "^4.17.21"
  }
}
